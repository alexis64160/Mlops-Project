# this file is a template for $DSDC_DIR/.secrets file (which must not be stored on git)
# .secret file defines main usernames and passwords for applications.
# install script will create a local personalized .secrets file that is to be modified by user and NEVER BE PUSHED ON GIT.

# Airflow
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow_strong_password
AIRFLOW_FERNET_KEY=your_fernet_key_here
_AIRFLOW_WWW_USER_CREATE=true

# Grafana
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin_strong_password

# MLflow
MLFLOW_TRACKING_PASSWORD=mlflow_strong_password

# PgAdmin
PGADMIN_DEFAULT_EMAIL=pgadmin@example.com
PGADMIN_DEFAULT_PASSWORD=pgadminpassword

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_strong_password
POSTGRES_DB=postgres

# dsdc data such as processed text, image reference paths or
# even computed embeddings are store on a specific postgres database.
# 3 following lines define this db and its owner crednetials.
POSTGRES_DSDC_USER=dsdc_service
POSTGRES_DSDC_PASSWORD=dsdc_service_strong_password
POSTGRES_DSDC_DB=dsdc

# airflow uses postgres db. These credentials are dedicated 
# to airflow user, who will be owner of his database.
POSTGRES_AIRFLOW_USER=airflow
POSTGRES_AIRFLOW_PASSWORD=airflow_strong_password
POSTGRES_AIRFLOW_DB=airflow

# mlflow uses postgres db. These credentials are dedicated 
# to mlflow user, who will be owner of his database.
POSTGRES_MLFLOW_USER=mlflow
POSTGRES_MLFLOW_PASSWORD=mlflow_strong_password
POSTGRES_MLFLOW_DB=mlflow

# data export has been centralized via a single user
# who will be granted read-only rights on dsdc, mlflow and airflow dbs.
POSTGRES_EXPORTER_USER=dsdc_exporter
POSTGRES_EXPORTER_PASSWORD=dsdc_exporter_strong_password
